# 舌体分割模型选型

> **ResNet34-UNet 目前不是经过多模型横向实验“选出来的最优模型”，而是在 D3-A 阶段被确定为“第一版分割 Baseline”的工程选型。**

也就是说，我们做过的是**基线选型**，还没有做正式的**模型选优**。

## 1. 这个选型是在哪一步发生的？

时间线其实是：

```text
D2
数据准备完成
   ↓
D3-A
定义“舌体分割到底怎么做”
   ↓
【这里第一次提出模型候选】
U-Net + ImageNet pretrained encoder
   ↓
推荐第一候选：
ResNet34-UNet
   ↓
D3-A Freeze
把它写进 segmentation contract
   ↓
D3-B
真正实现 ResNet34-UNet
   ↓
Tiny Overfit PASS
   ↓
D3-C
即将正式训练
```

也就是说，**模型架构的决定主要发生在 D3-A「Segmentation Dataset & Training Contract」阶段**。

当时我给你的 D3-A 提示词里专门有一节：

> ```
> Baseline architecture contract
> ```

里面给出的思路是：

```text
U-Net
+
ImageNet pretrained encoder
```

第一候选建议：

```text
ResNet34-UNet
```

当时也提到过 MobileNetV3-UNet 可以作为另一个轻量候选，但明确要求：

> **第一阶段不要一次比较很多架构，先建立一个可靠、标准、容易解释的 baseline。**

然后在你完成 D3-A 时，你把它写进了：

```text
configs/segmentation_v1.yaml
```

作为：

```text
BCE + Dice loss
ResNet34-UNet
```

的 D3-B 预定义契约。

到了 **D3-B**，才真正把这个设计落成代码：

```text
model.py
train_config.py
training/
```

并最终得到你现在这个：

```text
ResNet34-UNet
ImageNet pretrained
24,436,369 parameters
```

所以可以区分成：

| 阶段             | 做的事                                   |
| ---------------- | ---------------------------------------- |
| D3-A             | **决定先用什么 Baseline**                |
| D3-B             | **把这个 Baseline 真正实现并验证能训练** |
| D3-C             | **正式训练并得到真实性能**               |
| D3-D（如果需要） | **才可能真正做模型比较/优化选型**        |

------

# 2. 为什么当时会选 U-Net？

首先是因为你的任务本质上是：

> **二值语义分割。**

输入：

```text
舌头照片
```

输出不是：

```text
“有舌头 / 没舌头”
```

而是每一个像素：

```text
这个像素属于舌体吗？
```

最终需要：

```text
384 × 384
       ↓
每个像素预测
       ↓
Tongue Mask
```

U-Net 本来就是非常典型的像素级医学图像分割结构。

它的一个核心特点是：

```text
Encoder
提取语义

   ↓

Decoder
恢复空间位置
```

同时通过 skip connection：

```text
Encoder浅层特征 ─────────────┐
                           ↓
Encoder深层特征 → Decoder → Mask
```

把浅层的：

- 边缘；
- 轮廓；
- 纹理；
- 空间位置；

重新提供给 Decoder。

对于你这个任务尤其重要，因为你需要的不是“知道图里有舌头”，而是：

> **把舌头边缘尽可能准确地抠出来。**

------

# 3. 为什么不是直接用普通 ResNet34？

因为普通 ResNet34 更擅长：

```text
图片
 ↓
特征
 ↓
类别
```

例如：

```text
输入舌图

→ ResNet34

→ crack / no crack
```

这是分类。

但你现在需要：

```text
输入舌图

→ ???

→ 384 × 384 Mask
```

所以只有 Encoder 不够。

于是：

```text
ResNet34
   ↓
作为 Encoder
```

再接：

```text
U-Net Decoder
```

就变成：

> **ResNet34-UNet**

因此这个名字其实可以拆开理解：

```text
ResNet34-UNet
│          │
│          └── 分割框架
│
└──────────── 特征提取 Backbone
```

------

# 4. 为什么 Encoder 又选 ResNet34？

这里主要是几个工程因素综合考虑。

### 第一，你的分割训练数据并不大

目前真正作为分割监督的是：

```text
BioHit       300
TongueSet3   999

总计         1299
```

其中 train：

```text
1039张
```

这种数据量从零训练一个视觉 Backbone 风险比较大。

所以我们希望：

```text
ImageNet pretrained
        ↓
已经学会大量基础视觉特征
        ↓
再针对舌头做 Fine-tune
```

ResNet34 有成熟的 ImageNet 预训练权重。

------

### 第二，ResNet34 的规模比较适中

如果选：

```text
ResNet18
```

更轻，但表达能力稍弱。

如果一开始就上：

```text
ResNet101
ResNet152
ConvNeXt-Large
ViT-Large
```

对你这个1000多张训练图的 baseline 来说明显没有必要。

ResNet34 属于一个比较好的中间点：

```text
不是特别小
也不是特别重
```

最终你的整个 ResNet34-UNet：

```text
24.4M parameters
```

对于 RTX 4090 完全可控。

------

# 5. 为什么用了 ImageNet 预训练？

这个甚至比“为什么是34层”更重要。

因为你没有几十万张舌头分割图。

如果：

```text
ResNet34
随机初始化
↓
1039张训练图
```

网络不仅要学：

```text
什么是舌头
```

还要从头学：

```text
边缘
颜色变化
曲线
形状
纹理
局部结构
```

而 ImageNet 预训练 Backbone 已经学过大量通用视觉表示。

所以变成：

```text
通用视觉能力
       +
1039张舌头分割数据
       ↓
舌体分割能力
```

这对小规模医学图像任务是很自然的 baseline 策略。

------

# 6. 为什么没有直接用 SAM / SAM2？

这个我们在 D3-A 里也是明确讨论过的。

SAM 很强，但它解决的问题和你的最终工程需求不完全一样。

你需要的是：

```text
用户拍照
 ↓
自动执行
 ↓
固定输出 Tongue Mask
 ↓
低成本
 ↓
可部署
 ↓
进入后续舌色/裂纹/齿痕流水线
```

你真正需要的是一个**自动、固定、轻量、可训练的专用舌体分割器**。

而不是：

```text
大型通用 Promptable Segmentation Foundation Model
```

所以我们的策略是：

> SAM 可以以后作为 teacher、标注辅助、比较模型，但不把它作为第一版生产型 baseline。

------

# 7. 为什么没有直接上 SegFormer / DeepLabV3+？

不是因为我们已经证明它们比 U-Net 差。

**我们根本还没有比较。**

这点很重要。

当时的策略是：

```text
先选一个成熟 Baseline
↓
验证整个数据和训练链路
↓
跑出正式结果
↓
如果已经很好
就没有必要为了“模型更新”而换模型
↓
如果不够好
再做有针对性的比较
```

否则很容易一上来：

```text
UNet
DeepLabV3+
UNet++
SegFormer
Swin-UNet
SAM
...
```

每个都跑一次。

最后即使某一个分数最高，也不知道提升来自：

- Backbone；
- augmentation；
- loss；
- 学习率；
- 参数量；
- resize；
- 训练轮数；

中的哪一个。

------

# 8. 所以现在其实还没有完成“模型选型实验”

严格来讲，目前发生的是：

### Baseline selection

```text
任务：
舌体二值分割

↓     

选择成熟范式：
Encoder-Decoder

↓

选择经典框架：
U-Net

↓

选择中等规模预训练 Backbone：
ResNet34

↓

得到：
ResNet34-UNet
```

但是还没有：

### Architecture selection experiment

```text
ResNet34-UNet       Dice ?
DeepLabV3+          Dice ?
UNet++              Dice ?
SegFormer-B0        Dice ?
MobileNetV3-UNet    Dice ?
```

所以你以后面试的时候，千万不要说：

> “我对比了很多模型，最后发现 ResNet34-UNet 最好。”

因为目前这不是事实。

正确说法应该是：

> **在舌体分割阶段，我首先选择 ResNet34-UNet 作为 baseline。主要考虑到 U-Net 对像素级医学图像分割比较成熟，skip connection 有利于保留舌体边界信息；同时当前人工分割数据只有约1300张，因此使用 ImageNet 预训练的 ResNet34 作为编码器，在模型容量和小样本迁移能力之间做平衡。先通过 tiny-set overfit 验证训练链路，再通过冻结的 train/val/test 进行正式 baseline 评估。如果 baseline 跨域效果不足，再进入后续模型比较，而不是一开始进行无目的的架构搜索。**

这个回答其实比“我试了十个模型最后挑了一个”更有工程逻辑。

------

# 9. 那接下来的 D3-C 为什么特别重要？

因为**现在我们还不知道 ResNet34-UNet 是否真的适合你的数据**。

我们目前只知道：

```text
16张图
↓
Dice 0.951

说明：
它能学
```

但这不代表：

```text
130张完全没见过的 test
↓
它也能很好分割
```

所以 D3-C 才是第一次回答：

> **这个选型到底够不够好？**

如果正式结果：

```text
Overall Dice       0.97
BioHit Dice        0.98
TongueSet3 Dice    0.96
```

那我很可能会建议：

> 不要为了“模型高级”而换模型，ResNet34-UNet 已经足够，直接进入 ROI Pipeline。

但如果：

```text
BioHit       0.97
TongueSet3   0.84
```

那说明：

> 模型在受控数据上很好，但真实/手机域泛化不足。

这时候 D3-D 才应该正式做模型选型和优化，例如：

```text
ResNet34-UNet baseline
       VS
DeepLabV3+
       VS
SegFormer-B0
```

或者先研究 augmentation/domain shift。

------

## 所以最核心的结论

**ResNet34-UNet 的选型发生在 D3-A。**

它当时不是作为“实验选出的最优模型”，而是作为：

> **一个成熟、稳健、适合小样本、可利用 ImageNet 预训练、计算成本适中且非常适合作为医学图像分割基准的第一版 Baseline。**

D3-B 已经证明：

> **它能正常学习。**

而接下来的 D3-C 才会证明：

> **它在你的舌诊数据上到底够不够好。**

如果 D3-C 结果不够好，**D3-D 才是真正意义上的“模型优化/模型比较选型阶段”**。