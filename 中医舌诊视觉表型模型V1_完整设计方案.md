# 中医舌诊视觉表型模型 V1：完整设计方案

> 文档定位：舌诊模块的产品目标、数据边界、模型架构、训练方案、输出协议与实施路线基线。  
> 核心原则：**V1 不直接做疾病诊断或中医证候判断，而是把用户舌图转化为可靠、结构化、可解释的舌象视觉表型证据。**

---

## 1. 项目目标

### 1.1 一句话目标

输入一张用户拍摄的舌头照片，系统首先判断图像是否具备分析条件并定位舌体区域，然后对舌质、舌苔和局部舌象特征进行识别，最终输出统一的结构化舌象结果。

整体链路：

```text
用户舌图
  ↓
输入有效性检查
  ↓
舌体分割
  ↓
染苔/基础质量检查
  ↓
舌质分析
  ├─ 舌色
  └─ 舌形
  ↓
舌苔分析
  ├─ 苔色
  └─ 苔质
  ↓
局部特征分析
  ├─ 裂纹
  ├─ 齿痕
  ├─ 红点
  └─ 瘀斑
  ↓
统一置信度与异常处理
  ↓
结构化舌象结果
```

### 1.2 V1 的准确定位

V1 是：

> **Tongue Visual Phenotype Observer（舌象视觉表型观察器）**

它负责回答：

- 图片里是否有可分析的舌体；
- 舌体在哪里；
- 是否存在明显染苔或输入异常；
- 舌体颜色表现如何；
- 舌体形态表现如何；
- 舌苔颜色表现如何；
- 已有数据能够支持的苔质特征有哪些；
- 是否观察到裂纹、齿痕、红点、瘀斑；
- 各判断的模型置信度是多少；
- 哪些项目因图像质量或监督不足不能可靠判断。

V1 **不负责**直接回答：

- 用户属于什么中医证候；
- 用户是否“肝火旺”“脾虚湿盛”等；
- 用户患有什么疾病；
- 单凭舌图给出治疗方案或处方；
- 把五脏分区类标签直接解释为器官疾病。

这些高级判断应放到未来的“四诊融合/辨证层”。

---

# 2. 当前数据基础

当前已完成 8 个数据集的本地审计。以下事实以本地实际扫描结果为准，而不是二手汇总。

| 数据集 | 审计后唯一图 | 主要监督 | V1 角色 |
|---|---:|---|---|
| BioHit Tongue Image Dataset | 300 | 舌体人工 Mask | 分割预训练/评估 |
| TongueSet3 | 999 | 舌体人工 Mask | 自然环境分割与域泛化 |
| DSCT 裂纹舌 | 92 | 裂纹小样本分类 | 裂纹专项辅助/外部验证 |
| TMC-Tongue | 6575 | 多类舌象检测标注 | 核心视觉表型数据 |
| TongueDx | 5263 | 多标签图像级标注，约4650患者 ID | 核心多标签与自然环境泛化 |
| TonguExpert | 5992 | L1部分人工标签 + L2预测标签 + Mask/连续表型 | 核心人工补充 + 半监督候选 |
| Tooth-Marked Tongue | 1250 | 齿痕/非齿痕二分类 | 齿痕专项辅助 |
| 中医舌诊染苔数据 | 1935 | 染苔/非染苔场景标签 | 输入质控/域困难负样本 |

当前 8 集 canonical 扫描范围内：

- 未发现跨数据集 MD5 内容重复；
- 未发现损坏图；
- TMC 存在集内重复与跨原 train/val 的同图问题；
- TongueDx 存在重复文件，需要按 patient ID 而非按图片划分；
- TonguExpert L1 与 L2 必须严格区分，L2 预测标签不得作为最终金标准；
- 染苔数据只能用于食物染色等场景干扰识别，不得作为病理黄苔/黑苔等正样本。

---

# 3. V1 最终能够实现的能力

## 3.1 输入有效性检查

### 3.1.1 舌体是否存在

目标：

```text
用户图片
  ↓
是否存在足够完整的舌体
```

可输出：

- `tongue_detected`
- `tongue_area_ratio`
- `tongue_mask_valid`

主要依据：

- BioHit
- TongueSet3

### 3.1.2 舌体分割

输出舌体像素级 Mask，并裁剪 Tongue ROI。

```text
原始图片
  ↓
Segmentation Model
  ↓
Tongue Mask
  ↓
Tongue ROI
```

用途：

- 去除脸部、嘴唇、牙齿和背景干扰；
- 为舌色分析提供稳定区域；
- 为舌形分析提供轮廓；
- 为裂纹、红点等局部特征提供有效搜索空间；
- 为后续颜色校正和特征提取提供基础。

### 3.1.3 染苔检查

目标：

```text
疑似食物/饮料染色
vs
未发现明显染色
```

主要数据：

- 中医舌诊染苔数据

建议输出：

```json
{
  "stain_check": {
    "suspected": false,
    "confidence": 0.91
  }
}
```

重要边界：

**染苔检查 ≠ 完整图像质量检测。**

当前数据可以较明确支持“染苔场景识别”，但没有系统提供以下人工质量标签：

- 模糊；
- 过曝；
- 欠曝；
- 色温异常；
- 伸舌不足；
- 舌体遮挡；
- 严重偏转；
- 摄像头距离不合适。

其中一部分可先使用传统图像规则实现，例如：

- Laplacian variance：模糊程度；
- 灰度/亮度统计：曝光；
- 舌体面积占比：距离过远；
- Mask 完整度：舌体是否完整。

因此 V1 的 Quality Gate 推荐采用：

> **模型 + 图像规则混合方案**

而不是强行让一个神经网络承担所有质量判断。

---

# 4. 舌质分析

## 4.1 舌色

目标是描述可观察舌体颜色，而不是直接解释证候。

建议 V1 canonical ontology：

```text
tongue_body.color
├─ pale
├─ normal
├─ red
├─ dark
└─ purple
```

注意：

不同数据集的原始标签不完全等价。

### 当前标签来源示例

TMC-Tongue：

```text
hongshe → 红舌
zishe   → 紫舌
```

TongueDx：

```text
TonguePale
TipSideRed
```

TonguExpert L1：

```text
light
regular
dark
```

不能直接执行：

```text
dark = purple
TipSideRed = red tongue
```

因此统一数据时必须保留：

```text
source_dataset
source_label
canonical_label
mapping_status
mapping_confidence
mapping_note
```

其中语义不确定的映射宁可暂时保持独立，也不要强行合并。

---

## 4.2 舌形

根据目前数据，V1 建议只承诺：

```text
tongue_body.shape
├─ enlarged
└─ thin
```

必要时可加入：

```text
normal / no_obvious_abnormality
```

但“正常”标签必须谨慎定义，不能简单用“没有胖大/瘦薄标注”代替。

目前核心训练来源：

- TMC-Tongue

TMC 审计中：

- 胖大舌实例：703；
- 瘦舌实例：289。

V1 暂不扩展复杂舌形分类，除非获得更明确的人工监督。

---

# 5. 舌苔分析

舌苔需要拆成两个概念：

```text
舌苔
├─ 苔色 coating color
└─ 苔质 coating property
```

不能把它们混成一个单分类任务。

---

## 5.1 苔色

建议 V1：

```text
coating.color
├─ white
├─ light_yellow
├─ yellow
└─ black
```

可用来源：

### TMC-Tongue

审计实例数：

- 白苔：5167；
- 黄苔：1156；
- 黑苔：125。

### TonguExpert L1

人工标签中存在：

- white；
- light_yellow；
- yellow。

TonguExpert L1 数量有限，应主要作为人工高可信监督与独立校验，而不是简单覆盖所有 5992 张。

---

## 5.2 苔质

这是 V1 中必须更加保守的任务。

目前可考虑：

```text
coating.properties
├─ thick
├─ peeling
└─ greasy [experimental]
```

### 厚苔

TongueDx 存在 `FurThick` 标签，但数据分布极不均衡，因此需要检查其标签定义和泛化性能。

### 剥苔

TMC 存在剥苔舌标签，可作为候选任务。

### 腻苔

TonguExpert L2 有：

```text
non_greasy
greasy
greasy_thick
```

但 L2 是模型预测标签。

因此：

> 腻苔在 V1 中应标记为 experimental，不应与裂纹等人工监督充分的能力处于同一可信等级。

建议 V1 第一阶段甚至可以不向最终用户展示腻苔结果，仅保留实验接口。

---

# 6. 局部舌象特征

## 6.1 裂纹

目标分成两个层级：

### 一级：是否存在裂纹

```text
crack.present
```

主要来源：

- TMC-Tongue；
- TongueDx。

### 二级：裂纹严重程度

```text
crack.severity
├─ mild
└─ severe
```

辅助来源：

- TonguExpert L1；
- DSCT。

DSCT 数据量非常小，因此更适合：

- 小样本迁移；
- 专项验证；
- 外部 holdout evaluation。

而不适合作为主训练数据。

---

## 6.2 齿痕

同样分成两个层级：

### 一级：有无

```text
tooth_mark.present
```

来源：

- TMC-Tongue；
- TongueDx；
- Tooth-Marked Tongue。

### 二级：程度

```text
tooth_mark.severity
├─ mild
└─ severe
```

来源：

- TonguExpert L1。

Tooth-Marked 数据只有图像级有/无标签，没有定位或严重程度标注，因此只能作为二分类辅助。

---

## 6.3 红点

建议：

```text
red_spot.present
```

主要来源：

- TMC-Tongue；
- TongueDx `Spot`。

TMC 审计中红点实例数量较多，可作为 V1 较重要的局部特征任务。

---

## 6.4 瘀斑

建议：

```text
ecchymosis.present
```

主要来源：

- TongueDx `Ecchymosis`。

TongueDx 当前去重后的数据中：

- 正样本 471；
- 负样本 4638。

属于明显不平衡任务，训练时需要：

- class weighting；
- focal loss 或其他不平衡处理；
- PR-AUC 而不是只看 accuracy；
- 单独阈值校准。

---

# 7. 不纳入 V1 的标签

## 7.1 TMC 五脏区域类标签

暂时排除：

```text
shenquao
shenqutu
gandanao
gandantu
piweiao
xinfeitu
xinfeiao
```

理由：

1. 医学含义比纯视觉表型更强；
2. 部分类别极度稀有；
3. TMC 当前类名文件还存在 id 18/19 漂移问题；
4. V1 目标是“观察”，不是“器官映射”。

其中部分实例仅为 2 或 9，不能形成可靠监督能力。

---

## 7.2 TongueDx 五脏标签

暂时排除：

```text
Heart
Lung
Spleen
Liver
Kidney
```

尤其 `Spleen` 标签高度失衡：

```text
0 = 32
1 = 5077
```

仅凭高 Accuracy 无法说明模型有真实识别能力。

---

## 7.3 疾病诊断标签

V1 不做：

```text
tongue image → diabetes
tongue image → fatty liver
tongue image → disease diagnosis
```

未来若进行疾病关联研究，应作为单独实验层，不污染核心舌象模型。

---

# 8. 推荐系统架构

## 8.1 产品层采用两级架构

```text
                     用户照片
                        │
                        ▼
               Tongue Input Guard
                        │
       ┌────────────────┼────────────────┐
       │                │                │
     舌体分割        基础质量规则       染苔检查
       │                │                │
       └────────────────┼────────────────┘
                        │
                    是否可分析
                    /       \
                  否          是
                  ↓           ↓
              返回重拍建议   TongueNet
                              │
             ┌────────────────┼────────────────┐
             │                │                │
           舌质             舌苔            局部特征
             │                │                │
       舌色 / 舌形      苔色 / 苔质    裂纹/齿痕/红点/瘀斑
             └────────────────┼────────────────┘
                              │
                              ▼
                     Structured Result
```

这样比“一个模型从原图直接输出所有内容”更可靠。

---

# 9. 推荐模型架构

## 9.1 一个共享视觉主干 + 多任务 Head

算法核心仍然可以训练为一个 TongueNet：

```text
                      Tongue ROI
                          │
                          ▼
                  Shared Vision Encoder
                          │
        ┌─────────┬───────┼────────┬─────────┐
        ↓         ↓       ↓        ↓         ↓
      Body      Coating  Crack   ToothMark  Other
      Head       Head    Head      Head      Head
```

Encoder 可以后续通过实验选择：

- ConvNeXt；
- Swin Transformer；
- ViT；
- 其他适合中小规模医学图像训练的 backbone。

V1 不需要一开始追求最大模型，重点是：

- 标签语义正确；
- 数据划分正确；
- 多任务损失正确；
- 独立测试可信。

---

# 10. 为什么不能做普通多分类模型

错误方案：

```text
image → 20-class softmax
```

原因：

### 10.1 一张舌图可以同时有多个特征

例如：

```text
红舌
+ 胖大
+ 齿痕
+ 白厚苔
```

这些不是互斥类别。

### 10.2 数据集只标了部分任务

TonguExpert L1 大量存在 `NA`。

`NA` 表示：

> 没有该项监督信息。

不能解释成：

> 该特征不存在。

### 10.3 不同数据有不同监督形式

当前包含：

- segmentation mask；
- detection box；
- image-level multi-label；
- binary classification；
- severity classification；
- 场景标签。

因此必须使用多任务架构。

---

# 11. Masked Multi-task Training

为每个训练样本维护：

```text
label value
+
label available mask
```

例如某图片：

```text
tongue_color = red
crack = true
tooth_mark = NA
coating_color = NA
```

则：

```text
color loss       → 计算
crack loss       → 计算
toothmark loss   → 不计算
coating loss     → 不计算
```

整体损失形式：

```text
L_total =
Σ mask_task × weight_task × L_task
```

这样可以把不同来源的数据用于同一个共享 Encoder，而不会把“未知”错误训练成“阴性”。

---

# 12. 数据集到任务的映射

| 任务 | 主数据 | 辅助数据 | 备注 |
|---|---|---|---|
| 舌体分割 | TongueSet3 | BioHit | 自然环境 + 受控环境 |
| 染苔 | 染苔数据 | — | Quality Gate |
| 舌色 | TMC / TongueDx | TonguExpert L1 | 先统一 ontology |
| 舌形 | TMC | — | V1 重点胖大/瘦薄 |
| 苔色 | TMC | TonguExpert L1 | 白/浅黄/黄/黑 |
| 厚苔 | TongueDx | — | 严重类不平衡 |
| 剥苔 | TMC | — | 检查语义和实例 |
| 腻苔 | — | TonguExpert L2 | 实验，不作为金标准 |
| 裂纹有无 | TMC / TongueDx | — | 核心任务 |
| 裂纹程度 | TonguExpert L1 | DSCT | DSCT更适合外部验证 |
| 齿痕有无 | TMC / TongueDx | Tooth-Marked | Tooth-Marked 为辅助 |
| 齿痕程度 | TonguExpert L1 | — | 小规模人工标签 |
| 红点 | TMC / TongueDx | — | 核心任务 |
| 瘀斑 | TongueDx | — | 不平衡任务 |

---

# 13. Dataset Ontology v1

建议建立独立配置文件：

```text
ontology/
└─ tongue_phenotype_v1.yaml
```

结构示例：

```yaml
tongue_body:
  color:
    pale:
    normal:
    red:
    dark:
    purple:

  shape:
    enlarged:
    thin:

coating:
  color:
    white:
    light_yellow:
    yellow:
    black:

  properties:
    thick:
    peeling:
    greasy:

features:
  crack:
    present:
    severity:
      mild:
      severe:

  tooth_mark:
    present:
    severity:
      mild:
      severe:

  red_spot:
    present:

  ecchymosis:
    present:
```

每个源标签另建映射：

```text
mappings/
├─ tmc.yaml
├─ tonguedx.yaml
├─ tonguexpert.yaml
├─ tooth_marked.yaml
└─ dsct.yaml
```

禁止直接修改原始标签。

---

# 14. 数据清洗要求

## 14.1 RAW 永远只读

推荐：

```text
data/
├─ raw/
├─ audited/
├─ processed/
├─ splits/
├─ ontology/
└─ manifests/
```

`raw/`：

- 不删除；
- 不改名；
- 不覆盖标签；
- 不重新编码图片。

---

## 14.2 TMC 必须重新去重和划分

当前审计发现：

- canonical 6719 图；
- unique MD5 6575；
- 存在 144 个重复内容；
- 存在 train ↔ val 同图；
- classes 与 yaml 存在标签名称漂移。

因此不能直接把官方 split 当最终实验基线。

应：

```text
读取原始标注
  ↓
MD5 去重
  ↓
修复/锁定类名表
  ↓
检查标注冲突
  ↓
重新生成 train / val / test
```

---

## 14.3 TongueDx 必须按 patient ID 划分

当前约有：

```text
4650 patient id
```

同一 patient 的所有图片必须进入同一个 split。

禁止：

```text
同一人的图片 A → train
同一人的图片 B → test
```

否则模型会发生身份泄漏。

---

## 14.4 TonguExpert 必须分 L1 / L2

```text
L1 = 人工标签
L2 = 模型预测标签
```

要求：

- L1 可以进入人工监督；
- L2 不进入最终 test；
- L2 不作为唯一 val；
- L2 后续只做半监督/伪标签；
- `NA` 不得转成 0。

---

# 15. 推荐训练阶段

## Stage 0：数据工程基线

目标：

- 完成 ontology；
- 完成所有标签 mapping；
- 完成去重；
- 完成 patient-level split；
- 生成统一 manifest；
- 固定数据版本。

输出：

```text
dataset_manifest_v1.parquet
split_manifest_v1.json
tongue_phenotype_v1.yaml
```

---

## Stage 1：舌体分割

训练：

```text
TongueSet3
+
BioHit
```

目标：

- Tongue Dice；
- IoU；
- 自然图像泛化。

输出：

```text
tongue_segmentation_v1
```

---

## Stage 2：Input Guard

加入：

```text
染苔数据
+
传统图像质量规则
```

输出：

```text
usable
stain_suspected
blur_score
exposure_score
tongue_area_ratio
```

---

## Stage 3：核心表型模型

主训练数据：

```text
TMC cleaned
+
TongueDx
+
TonguExpert L1
```

核心任务：

```text
舌色
舌形
苔色
厚苔
剥苔
裂纹
齿痕
红点
瘀斑
```

模型：

```text
shared encoder + masked multi-task heads
```

---

## Stage 4：专项增强

加入：

```text
Tooth-Marked
→ tooth mark auxiliary training

DSCT
→ crack severity / external evaluation
```

这里不要求所有专项数据同时更新全部 Head。

---

## Stage 5：半监督实验

只有在核心模型稳定后考虑 TonguExpert L2。

策略：

```text
L2 prediction
  ↓
高置信度筛选
  ↓
pseudo-label training
  ↓
人工 L1 / 独立 test 验证是否真的改善
```

如果不能提高真实人工测试集性能，则不采用。

---

# 16. 评估指标

不同任务不能统一用 Accuracy。

## 16.1 分割

使用：

- Dice；
- IoU；
- boundary quality；
- failure rate。

---

## 16.2 二分类/多标签

使用：

- Precision；
- Recall；
- F1；
- ROC-AUC；
- PR-AUC；
- sensitivity / specificity。

类别严重不平衡时优先关注：

> PR-AUC、Recall、Precision、F1

而不是 Accuracy。

---

## 16.3 多分类

例如苔色：

- Macro-F1；
- per-class Recall；
- confusion matrix；
- balanced accuracy。

---

## 16.4 概率可信度

最终产品输出 confidence，因此需要：

- calibration curve；
- Expected Calibration Error；
- threshold tuning。

模型预测 `0.90` 应尽量具有可解释的概率意义，而不是未经校准的 softmax 分数。

---

# 17. 最终结构化输出协议

建议 API 输出：

```json
{
  "version": "tongue-v1",
  "quality": {
    "usable": true,
    "tongue_detected": true,
    "tongue_area_ratio": 0.42,
    "stain_suspected": false,
    "blur_score": 0.08,
    "exposure_status": "normal"
  },

  "tongue_body": {
    "color": {
      "label": "red",
      "confidence": 0.86
    },
    "shape": {
      "enlarged": {
        "present": true,
        "confidence": 0.79
      },
      "thin": {
        "present": false,
        "confidence": 0.94
      }
    }
  },

  "coating": {
    "color": {
      "label": "white",
      "confidence": 0.91
    },
    "properties": {
      "thick": {
        "present": true,
        "confidence": 0.82
      },
      "peeling": {
        "present": false,
        "confidence": 0.88
      }
    }
  },

  "features": {
    "crack": {
      "present": true,
      "confidence": 0.94,
      "severity": {
        "label": "mild",
        "confidence": 0.72
      }
    },

    "tooth_mark": {
      "present": true,
      "confidence": 0.88
    },

    "red_spot": {
      "present": false,
      "confidence": 0.83
    },

    "ecchymosis": {
      "present": false,
      "confidence": 0.73
    }
  },

  "warnings": []
}
```

---

# 18. 不确定结果必须允许返回 Unknown

模型不能被迫回答所有问题。

例如：

```json
{
  "coating": {
    "color": {
      "label": "unknown",
      "confidence": 0.41
    }
  }
}
```

可能原因：

- 图片色温异常；
- 舌苔区域遮挡；
- 模型置信度不足；
- 当前 ontology 没有覆盖该表型；
- 属于训练分布之外。

允许 `unknown` 比错误地给出一个确定标签更可靠。

---

# 19. UI 最终展示建议

不要只给用户一个 JSON。

可以显示：

```text
图像质量：可分析

舌质
- 舌色：偏红（86%）
- 舌形：偏胖大（79%）

舌苔
- 苔色：白（91%）
- 厚苔：疑似（82%）

局部特征
- 裂纹：发现（94%），程度偏轻
- 齿痕：发现（88%）
- 红点：未明显发现（83%）
- 瘀斑：未明显发现（73%）
```

同时显示：

- 舌体轮廓；
- 有检测标注条件时显示局部区域；
- 低置信度结果标记为“无法可靠判断”。

注意措辞应使用：

> “模型观察到……”

而不是：

> “你患有……”

---

# 20. V1 的验收定义

V1 完成不等于所有指标都达到临床诊断级。

建议 V1 工程验收条件：

### 数据层

- 8 集全部建立 manifest；
- TMC 完成去重；
- TongueDx 完成 patient-level split；
- TonguExpert L1/L2 严格隔离；
- ontology v1 固定；
- 每个 canonical label 能追溯到 source label。

### 模型层

- 舌体分割模型可稳定运行；
- 输入 Guard 可拒绝明显无效输入；
- 染苔模型可单独评估；
- 核心表型模型实现多任务训练；
- 缺失标签 Mask 正确；
- 每个任务都有独立指标；
- 置信度阈值可配置。

### 系统层

输入：

```text
1 张 JPG / PNG 舌图
```

输出：

```text
质量判断
+
Tongue Mask
+
舌质结果
+
舌苔结果
+
局部特征结果
+
置信度
+
warnings
```

---

# 21. V1 与未来四诊系统的关系

舌诊模块最终不应该直接与其他模型互相耦合。

它应该只提供标准证据：

```text
Tongue Image
  ↓
Tongue Observer
  ↓
TongueEvidence
```

未来：

```text
舌诊 TongueEvidence ───────┐
面诊 FaceEvidence ─────────┤
声诊 VoiceEvidence ────────┤
问诊 InterviewEvidence ────┼→ 四诊融合 / 辨证 Agent
脉诊 PulseEvidence ─────────┘
```

因此舌诊的输出对象应该成为稳定接口，而不是把辨证逻辑写进视觉模型。

---

# 22. 推荐工程目录

```text
tongue-diagnosis/
│
├─ data/
│  ├─ raw/
│  ├─ audited/
│  ├─ processed/
│  ├─ manifests/
│  └─ splits/
│
├─ ontology/
│  ├─ tongue_phenotype_v1.yaml
│  └─ mappings/
│
├─ src/
│  ├─ quality/
│  ├─ segmentation/
│  ├─ models/
│  ├─ losses/
│  ├─ datasets/
│  ├─ inference/
│  └─ api/
│
├─ configs/
│  ├─ segmentation/
│  ├─ multitask/
│  └─ thresholds/
│
├─ tests/
│
├─ reports/
│  ├─ dataset_audit/
│  ├─ experiments/
│  └─ evaluation/
│
└─ docs/
```

---

# 23. 当前最优实施顺序

不建议现在直接开始“训练大模型”。

正确顺序是：

```text
D0 目标和数据契约固定
↓
D1 Ontology 与统一 manifest
↓
D2 数据清洗 / 去重 / split
↓
D3 舌体分割 baseline
↓
D4 Input Guard + 染苔 baseline
↓
D5 核心多任务 phenotype baseline
↓
D6 专项增强：裂纹 / 齿痕
↓
D7 阈值校准与统一推理 API
↓
D8 前端展示与完整端到端原型
↓
D9 独立验证与模型卡
```

其中 D0-D2 的价值不低于模型训练本身。

如果 ontology 和 split 错了，后面的指标即使很好也没有意义。

---

# 24. 最终结论

基于当前已经获得并审计的 8 个数据集，第一版舌诊系统具备实现以下完整链路的现实基础：

```text
用户舌图
  ↓
舌体识别与分割
  ↓
基础图像质量 + 染苔检查
  ↓
舌色分析
  ↓
舌形分析
  ↓
苔色分析
  ↓
有限苔质分析
  ↓
裂纹 / 齿痕 / 红点 / 瘀斑分析
  ↓
置信度与 unknown 机制
  ↓
统一结构化舌象结果
```

最适合的技术定位不是：

> “一张舌头照片直接诊断疾病的 AI”

而是：

> **“从真实用户舌图中稳定提取可观察舌象视觉表型，并输出标准化证据的多任务视觉模型。”**

这使它既能够独立作为一个完整的舌诊原型，又能在未来自然接入面诊、声诊、问诊和脉诊，成为四诊融合系统中的标准视觉证据模块。
